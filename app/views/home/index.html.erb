<div class="container">
  <h1>Home#index</h1>
  <div id="slider">
    <% @products.each do |product| %>
      <%= image_tag product.image.url, size: "1200x800", alt: product.name %>
    <% end %>
  </div>
</div>

